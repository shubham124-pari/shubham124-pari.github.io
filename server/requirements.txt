# ---------------------------------------------------
# Core web framework + database
# ---------------------------------------------------
Flask==3.1.3
Flask-Cors==5.0.0
PyMySQL==1.1.1
python-dotenv==1.2.2
Werkzeug==3.1.3

# ---------------------------------------------------
# Auth
# ---------------------------------------------------
PyJWT==2.7.0

# ---------------------------------------------------
# Private chat (real-time: typing indicators, online
# status, live message delivery, read receipts)
# ---------------------------------------------------
Flask-SocketIO==5.4.1
python-socketio==5.11.4
python-engineio==4.10.1

# ---------------------------------------------------
# File uploads (resume/certificate/photo/project images
# and chat image + document sharing)
# ---------------------------------------------------
Pillow==12.3.0

# ---------------------------------------------------
# Multilingual AI assistant — automatic language detection.
# Gemini/OpenAI themselves are called over plain HTTPS
# (urllib, stdlib only — see server/services/ai_provider.py)
# so no separate SDK package is required for either provider.
# ---------------------------------------------------
langdetect==1.0.9

# ---------------------------------------------------
# Testing only — not needed to run the app itself
# ---------------------------------------------------
pytest==8.3.4
cryptography>=42.0.0
cryptography>=42.0.0
google-auth==2.35.0
